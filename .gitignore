# Node
node_modules/
dist/
build/
.next/
.turbo/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Env
.env
.env.local
.env.*.local

# Generated — but we DO commit PNG/favicon exports so consumers don't have to run scripts
# (keep /logos/png and /logos/favicons tracked)

# Temp
tmp/
.cache/

# Wrangler local state (dev server cache, local KV/DO, build tmp).
# The deployable output is dist/, already ignored above.
.wrangler/

# 21st.dev registry — local render output from `npm run registry:render`,
# and the CLI's per-project design context (`21st init --design-context`).
registry/.renders/
.21st/
